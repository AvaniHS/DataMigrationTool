# Config platform web (P0)

```bash
cd config-platform/web
npm install
npm run dev
npm run test
```

Storybook (blueprint layout comparison):

```bash
npm run storybook
```

Set `VITE_API_URL` in `.env` (default `/api` via Vite proxy).
